# M5 冻结评测中期诊断与 M5.1 入口

日期：2026-07-24  
冻结协议哈希：`326214f43418c24d3a1a74f8cda8c69c4e87ade8bbdbb35f373216661ad35f4c`

## 1. 快照范围

本诊断基于本地 Windows/WGL 隔离进程评测的前 149/500 回合。冻结任务继续运行，
本文件只记录中期证据，不替代 500 回合最终报告，也不将当前结果标记为正式基线。

当前汇总：

- 目标获取率：97.99%
- 粗对准成功率：97.99%
- 细对准成功率：53.69%
- 全流程成功率：21.48%
- 80 个回合获得细对准授权
- 其中 41 个回合的评价真值轴角仍大于 5°，占 51.25%
- 48 个回合在获得细对准授权后未完成插入
  - 42 个因插入中的视觉授权缺失或撤销而停止
  - 6 个因视觉安全间隙不足而拒绝启动
- 未检测到戳卡内壁接触或过插

## 2. 五层随机化诊断

| 随机化层 | 回合 | 获取率 | 细对准成功率 | 全流程成功率 |
|---|---:|---:|---:|---:|
| geometry | 30 | 93.33% | 40.00% | 16.67% |
| photometric | 30 | 100.00% | 56.67% | 13.33% |
| calibration | 30 | 100.00% | 40.00% | 13.33% |
| occlusion | 30 | 100.00% | 83.33% | 53.33% |
| combined | 29 | 96.55% | 48.28% | 10.34% |

目标获取已经不是当前主瓶颈。主要问题集中在：

1. 单帧内外环尺度、椭圆姿态和深度估计缺乏跨帧一致性检验。
2. 固定外环残差标定在随机几何和相机标定扰动下不能可靠代表真实同轴状态。
3. 插入接近戳卡入口时，视觉特征尺度和遮挡快速变化，单帧失败会立即撤权并结束，
   尚无“停止运动、原位重新观察、恢复或安全终止”的状态。
4. 视觉门通过与评价真值轴角之间仍有明显偏差，不能交给强化学习掩盖。

## 3. M5.1 已建立的安全基础

新增 `TemporalFineObservationValidator`：

- 仅接收新鲜、质量门通过的观测；
- 对内外中心、横向、轴角和深度使用有限窗口中位数；
- 检查窗口内的变化范围；
- 缺帧或质量失败立即清空窗口，不用旧数据授权插入。

新增 `FailClosedReobservationGate`：

- 当前视觉无效时输出 `hold_and_reobserve`，机械臂必须保持原位；
- 新鲜视觉恢复后才能重新输出 `move`；
- 超过重观察预算输出 `abort`；
- 不允许在坏帧期间依靠宽限继续运动。

自动诊断脚本会从当前报告生成：

- `m5_checkpoint_diagnosis.json`
- `m5_1_replay_manifest.json`

复测清单以确定性规则从每个随机化层、每类结果中选择代表种子，后续 M5 与 M5.1
必须在相同种子和相同域参数下做配对比较。

## 4. 后续门槛

1. 完成原始 500 回合冻结评测并保存最终报告。
2. 对清单中的种子先做 M5 原版重放，验证可复现性。
3. M5.1 依次加入时序验证、原位重观察、主动尺度/姿态估计；每项单独消融。
4. 视觉授权不得使用 MuJoCo 位姿真值。真值继续只用于离线评价。
5. M5.1 至少解决“视觉门通过但真值轴角仍偏大”和“插入末段瞬时撤权”后，
   才冻结新的传统基线。
6. M6 只在新的传统基线上加入有界残差动作，安全门始终拥有撤权权力。

## 5. 配对重放入口

`run_m5_1_paired_replay.py` 已建立，输入为冻结报告和
`m5_1_replay_manifest.json`，并执行以下独立候选链：

1. 保留与 M5 相同的回合编号、随机种子、五层域参数和六轴 MuJoCo plant。
2. 外环主动姿态阶段完成后，继续使用内外环同心度与椭圆各向异性做主动视觉探测，
   不再执行一次性的固定角度残差动作。
3. 细对准授权前需要新鲜多帧窗口通过稳定性检查。
4. 插入每移动一步都清空旧窗口，并在当前位置重新收集至少三帧新鲜观测。
5. 无效观测只允许保持和重观察；恢复后才继续，预算耗尽则终止。
6. 输出 M5→M5.1 的同种子成功/失败迁移矩阵，以及接触、过插和撤权统计。

在 M5 的正式 WGL 批次运行期间只进行 `--list-only` 和单元测试，不并发启动第二套
MuJoCo 渲染任务。待 500 回合结束后先运行少量冒烟种子，再运行完整配对清单。
